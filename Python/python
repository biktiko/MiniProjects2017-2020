1) Метод get возвращает значение ключа если он присутсвует, а если нет то возвращает второй аргумент

picnicItems={"apples": 5, "cups": 2}
str(picnicItems.get("cups", 0)) //2
str(picnicItems.get("eggs", 0)) //0

2)Метод setdefault возвращает значение ключа() если он присутсвует, а если нет то добавляет новый ключ с значением (второй аргумент) в определённый словарь

spam= {"name": "Pooka", 'age': "black"}
spam.setdefault("color", "black")


